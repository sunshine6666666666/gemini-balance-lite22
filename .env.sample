# Backup API Keys for load balancing (comma-separated)
# 备用API Key池，用于负载均衡（逗号分隔）
# 当客户端只发送单个API Key时，系统会自动使用这个备用Key池进行负载均衡
# When client sends only single API key, system will automatically use this backup key pool for load balancing

BACKUP_API_KEYS=your_api_key_1,your_api_key_2,your_api_key_3,your_api_key_4,your_api_key_5,your_api_key_6,your_api_key_7,your_api_key_8,your_api_key_9,your_api_key_10,your_api_key_11,your_api_key_12

#--------------------------------------------------------------------------
# Security Configuration (安全配置)
# 重要：这些配置用于保护您的API Key池不被恶意使用
# Important: These configurations protect your API key pool from malicious use
#--------------------------------------------------------------------------

# Trusted API Keys (可信API Key白名单)
# 只有这些Key可以触发备用Key池的使用，用逗号分隔
# Only these keys can trigger the use of backup key pool, comma-separated
#
# 安全机制说明：
# - 当客户端发送单个API Key时，系统会检查该Key是否在此白名单中
# - 如果在白名单中：允许使用BACKUP_API_KEYS进行负载均衡
# - 如果不在白名单中：直接拒绝请求，返回401未授权错误
# - 当客户端发送多个API Key时：正常处理，不启用备用Key池
#
# Security mechanism explanation:
# - When client sends single API key, system checks if the key is in this whitelist
# - If in whitelist: Allow using BACKUP_API_KEYS for load balancing
# - If not in whitelist: Reject request directly, return 401 unauthorized error
# - When client sends multiple API keys: Process normally, backup key pool not activated
#
# 配置示例 / Configuration example:
TRUSTED_API_KEYS=your_trusted_key_1,your_trusted_key_2,your_trusted_key_3

# Important Notes (重要说明):
#
# BACKUP_API_KEYS:
# - Make sure to replace the example keys with your actual Google AI API keys
# - You can get API keys from: https://aistudio.google.com/app/apikey
# - Minimum 2 keys recommended, 12+ keys for best load balancing
# - 请确保将示例密钥替换为您的实际Google AI API密钥
# - 建议最少2个密钥，12个以上密钥可获得最佳负载均衡效果
#
# TRUSTED_API_KEYS:
# - 这是最重要的安全配置！只有白名单中的Key才能使用您的备用Key池
# - 建议添加您自己使用的API Key，或者您完全信任的用户的Key
# - 不要添加陌生人的API Key，否则他们可以免费使用您的配额
# - This is the most important security configuration! Only whitelisted keys can use your backup key pool
# - Recommend adding your own API keys or keys from users you completely trust
# - Do not add strangers' API keys, or they can use your quota for free
